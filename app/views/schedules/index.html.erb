<%= render "form" %>

<%= month_calendar events: @schedules do |date, schedules| %>
  <%= date %>

  <% schedules.each do |schedule| %>
    <div>
      <%= link_to schedule.name, edit_schedule_path(schedule.id), style: "text-decoration: none;"%>
    </div>
  <% end %>

<% end %>
